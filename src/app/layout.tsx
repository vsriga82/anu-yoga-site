import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Anu Mindfulness Yoga | Therapeutic Yoga in Chennai & Online",
    template: "%s | Anu Mindfulness Yoga",
  },
  description:
    "Personalized therapeutic yoga for hormonal health, chronic pain, and more — sessions in Chennai and online worldwide.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${playfair.variable} h-full antialiased scroll-smooth`}
    >
      <body className="min-h-full flex flex-col font-sans bg-beige text-slate-800">{children}</body>
    </html>
  );
}
